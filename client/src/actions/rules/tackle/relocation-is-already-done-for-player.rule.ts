import { Rule } from "../../classes/rule";

export class RelocationIsAlreadyDoneForPlayer implements Rule {
    validate(context: GameContext): boolean {
        return context.relocation.const [state, setstate] = useState<type>(initialValue);
    }
    errorMessage = "relocation isn't done for player";

}